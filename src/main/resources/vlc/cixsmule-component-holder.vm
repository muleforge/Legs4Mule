#**
Mule Component Holder Velocity Template.
A holder is a wrapper class used for methods with multiple input
or output structures.
@author Fady
@version 0.1
*#
#foreach($cixsOperation in $muleComponent.cixsOperations)
#if ($cixsOperation.name == $operation)
#parse("vlc/cixsmule-component-common-package.vm")
#if ($mode == "input")
#set($className = ${cixsOperation.requestHolderType})
#set($cixsStructures = $cixsOperation.input)
#else
#set($className = ${cixsOperation.responseHolderType})
#set($cixsStructures = $cixsOperation.output)
#end
#foreach($cixsStructure in $cixsStructures)
import ${cixsStructure.jaxbPackageName}.${cixsStructure.jaxbType};
#end

/**
 * LegStar/Mule Component holder.
 * Wraps multiple $mode structures. 
 * 
 * This class was generated by ${generatorName}.
 * Generated on $formattedDate
 */
public class ${className} {
#foreach($cixsStructure in $cixsStructures)
    /** Inner ${cixsStructure.jaxbType} JAXB-bound object. */
    private ${cixsStructure.jaxbType} ${cixsStructure.jaxbFieldName};
#end

#foreach($cixsStructure in $cixsStructures)
    /**
     * Get the inner ${cixsStructure.jaxbType} JAXB-bound object.
     * @return JAXB-bound object
     */
    public final ${cixsStructure.jaxbType} get${cixsStructure.jaxbPropertyName}() {
        return ${cixsStructure.jaxbFieldName};
    }

    /**
     * Set the inner ${cixsStructure.jaxbType} JAXB-bound object.
     * @param value JAXB-bound object
     */
    public final void set${cixsStructure.jaxbPropertyName}(
        final ${cixsStructure.jaxbType} value) {
        this.${cixsStructure.jaxbFieldName} = value;
    }
#end

}
#end
#end